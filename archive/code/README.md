# Code archive

This directory preserves historical and exploratory code that is not part of
the supported paper, appendix, Make, or HPC workflows. Nothing here is called
by the default pipeline. `manifest.csv` records each file's original path and
disposition. Archived files are retained for provenance and are not deleted.
