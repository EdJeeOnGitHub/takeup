# HPC launchers

Slurm jobs and submission wrappers are grouped by `structural/`, `policy/`,
and `appendix/`. Invoke them from the repository root. Old root-level launcher
paths were intentionally removed; see `docs/path-migration.md`.
