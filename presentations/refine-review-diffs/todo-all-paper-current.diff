diff --git a/ECM ReStud.tex b/ECM ReStud.tex
index 60be426..d4c654f 100644
--- a/ECM ReStud.tex	
+++ b/ECM ReStud.tex	
@@ -351,7 +351,7 @@ After selecting PoTs, we randomly assign them, stratified by county, to a Close
 
 \vspace{0.4em}
 \noindent\textbf{3.\ Targeted community selection.}
-To implement the Close/Far assignment, we randomly select one eligible community for each PoT. A community is eligible if (i) the distance between its centroid and the selected PoT falls within the assigned distance band and (ii) the community lies within the non-overlapping catchment area defined in Step 1.\footnote{Appendix~\ref{sec:site-selection} describes how this was implemented in practice using anchor points to guide field listing.} In all but two clusters, the assigned PoT was the nearest deworming site.
+To implement the Close/Far assignment, we randomly select one eligible community for each PoT. A community is eligible if (i) the distance between its centroid and the selected PoT falls within the assigned distance band and (ii) the community lies within the non-overlapping catchment area defined in Step 1.\footnote{Appendix~\ref{sec:site-selection} describes how this was implemented in practice using anchor points to guide field listing.} In the realized implementation data, the assigned PoT is the nearest deworming site for every selected community; in two clusters, the nearest-alternative margin is slightly below 0.5 km.
 
 The final implemented assignment generated an average distance difference of 1.02~km and increased median walking time from 15 to 30 minutes (Table~\ref{tab:dist-travel-treatment}).\footnote{Ninety percent of adults report walking to the PoT, while most others travel by bicycle.} In six cases, field constraints identified after enumeration but before implementation made the initially assigned PoT-community pairing infeasible. These assignments were re-randomized before any outcome data were observed, using the same pre-specified protocol and eligibility criteria. The realized assignment contains 80 Close and 64 Far PoTs. Appendix~\ref{sec:site-selection} details the realized allocation and shows that the imbalance is not statistically unlikely under equal allocation. Figure~\ref{fig:distance} shows the resulting distribution of community-centroid-to-PoT distances.
 
@@ -359,7 +359,7 @@ The final implemented assignment generated an average distance difference of 1.0
 \noindent\textbf{Distance as a continuous measure.} 
 Our primary reduced form distance treatment is the Close/Far assignment. We use realized community-centroid-to-PoT distance only in reduced form robustness checks, where it provides a continuous measure of the travel distance variation induced by the design. In the structural model, centroid-to-PoT distance is the main distance input, since it is the randomized spatial margin and a salient component of access costs. Specifically, we calculate the distance from each community's centroid to its assigned finalized PoT. All realized distance measures use the finalized PoT location at which treatment was delivered.
 
-Because PoTs are selected using a geographically constrained procedure, the induced assignment mechanism could in principle depend on local geography, such as school density. Following \citet{borusyak2023nonrandom}, we re-run the full selection and assignment algorithm 500 times to assess whether communities assigned to Close and Far in the realized experiment had similar potential distance distributions under counterfactual re-draws. These simulations provide no evidence of systematic differences in potential centroid-to-PoT distances across the realized Close and Far groups (Figure~\ref{fig:bh-dens-plot}). We further verify that realized centroid-to-PoT distance is uncorrelated with baseline covariates (Figure~\ref{fig:ri-baseline}). Taken together, these checks support interpreting realized community-centroid-to-PoT distance as a continuous measure of the as-good-as-random travel distance variation induced by the design.
+Because PoTs are selected using a geographically constrained procedure, the induced assignment mechanism could in principle depend on local geography, such as school density. Following \citet{borusyak2023nonrandom}, we re-run the full selection and assignment algorithm 500 times to assess whether communities assigned to Close and Far in the realized experiment had similar potential distance distributions under counterfactual re-draws. These simulations condition on the realized surveyed communities and reproduce the PoT selection, Close/Far assignment, targeted-community pairing, and feasibility restrictions used in the experiment; for each simulated draw, each realized community is assigned the distance it would have had under the simulated feasible PoT-community pairing. The resulting potential distance distributions are statistically indistinguishable across the realized Close and Far groups (Figure~\ref{fig:bh-dens-plot}). We further verify that realized centroid-to-PoT distance is uncorrelated with baseline covariates (Figure~\ref{fig:ri-baseline}). Taken together, these checks support interpreting realized community-centroid-to-PoT distance as a continuous measure of the as-good-as-random travel distance variation induced by the design, conditional on the fixed surveyed-community frame and the constrained assignment protocol.
 
 \subsection{Item and Public Signal Treatments}
 
@@ -562,7 +562,7 @@ Bracelets increase take-up by 7.5 percentage points relative to Control (\(p<0.0
 
 The distance interaction differs between public signals and the active comparison item. Calendar effects are essentially identical in Close and Far communities. By contrast, Bracelet and Ink effects are quantitatively  larger in Far communities, though the arm specific interactions are imprecisely estimated. Because the hypothesis concerns public observability, we test whether the Far--Close take-up gap is smaller in the public signal arms than in the no-signal arms. Pooling Bracelet and Ink as Any Signal and comparing them with Control and Calendar as No Signal, public signals reduce the Far--Close take-up gap by 6.8 percentage points (\(p=0.083\)). The same pattern appears in continuous distance specifications, where the pooled public signal by distance interaction is significant at the 5 percent level (Table~\ref{tab:rf-ates-cts-dist}). The discrete-distance results are also similar when we omit the selected controls and expected-distance control (Table~\ref{tab:rf-takeup-no-covs-no-mud}).
 
-The Calendar arm is informative for interpretation. A pure ``mass near the margin'' explanation would predict that any additive private item should have a larger effect in Far communities if more adults are close to the participation cutoff there. The Calendar effect does not vary with distance, while the distance interaction appears only for public signals. This pattern points to a mechanism tied to observability and social image rather than to a generic density at the margin effect.
+The Calendar arm is informative for interpretation. A pure ``mass near the margin'' explanation would predict that any additive private item should have a larger effect in Far communities if more adults are close to the participation cutoff there. The Calendar effect does not vary with distance, while the distance interaction appears only for public signals. This pattern also helps address the baseline imbalance in past deworming reported in Appendix Table~\ref{tab:baseline-bal}: Calendar communities display a similar Far--Close pattern in prior deworming to the public-signal arms, but Calendar take-up is flat across distance. Moreover, although the baseline and take-up samples cannot be linked at the individual level, cluster-level baseline prior-deworming rates can be merged to the full take-up sample. Controlling for the cluster shares ever dewormed and dewormed in the past year does not attenuate the pooled public-signal distance interaction; in the corresponding check, it increases from 7.2 to 10.2 percentage points. Using endline self-reported deworming as an alternative outcome produces the same direction of change, with the interaction increasing from 5.9 to 8.6 percentage points (Appendix Table~\ref{tab:prior-deworming-robustness}). This pattern points to a mechanism tied to observability and social image rather than to a generic density at the margin effect or differential prior deworming propensity.
 
 Figure~\ref{fig:np-rf} plots community level take-up against community centroid to PoT distance, fitting flexible penalized splines separately by item/signal arm. Take-up declines with distance in all arms, but the Control curve falls steeply and the Bracelet curve remains comparatively high at longer distances. Ink is noisier and remains low, consistent with its muted average take-up effect. These descriptive patterns mirror the regression evidence that the distance gradient is attenuated when participation is made publicly visible, and motivate the structural decomposition.
 
@@ -656,6 +656,7 @@ Figure~\ref{fig:harmonized-delta} illustrates how social image returns, \(\mu(d)
 Idiosyncratic shocks \(u_i\) blur type inference. When the dispersion of \(u_i\) is small, actions closely reveal \(v_i\). As this dispersion rises, observers cannot tell whether an individual deworms because of intrinsic motivation or because of a favorable shock. Social image returns fall at every cutoff, and the returns curve flattens.
 
 Panel (b) allows observability to fall with distance, \(\mu'(d)<0\). If the decline in \(\mu(d)\) dominates the rise in \(\Delta\), then the social image return can fall with distance: at large \(d\), only highly motivated individuals deworm, but their action is less visible, so the reputational return \(\mu(d)\Delta(w^\ast(d))\) is small.
+For any fixed cutoff in the plotted range, the lower-noise curves remain above the higher-noise curves; Panel (b) changes the level and slope of the return by allowing \(\mu(d)\) to vary with distance, not by reversing the ordering of \(\Delta(w^\ast)\) across \(\sigma_u\).
 
 \begin{figure}[htbp]
     \centering
@@ -865,7 +866,7 @@ The relative valuation exercise in no-item Control communities identifies the pr
 Holding distance fixed, variation across item/signal arms in reported observability disciplines \(\beta^{O}_{z}\). Within an item/signal arm, variation across distance disciplines \(\gamma^{O}_{z}\).
 
 \emph{Noise parameter \(\sigma_u\).}
-The parameter \(\sigma_u\) governs how informative deworming decisions are about intrinsic motivation. We normalize \(v_i\sim N(0,1)\) and assume \(u_i\sim N(0,\sigma_u^2)\), so \(w_i=v_i+u_i\sim N(0,1+\sigma_u^2)\). For the normal mixture, the type inference term is
+The parameter \(\sigma_u\) governs how informative deworming decisions are about intrinsic motivation. We normalize \(v_i\sim N(0,1)\) and assume \(u_i\sim N(0,\sigma_u^2)\), so \(w_i=v_i+u_i\sim N(0,1+\sigma_u^2)\). Under this additive-normal specification, the type inference term is
 \[
 \Delta(w)
 =
@@ -876,7 +877,7 @@ The parameter \(\sigma_u\) governs how informative deworming decisions are about
 x\equiv \frac{w}{\sqrt{1+\sigma_u^{2}}},
 \]
 where \(\phi\) and \(\Phi\) denote the standard normal pdf and cdf.\footnote{A derivation of this expression, and an analogous expression with bounded \(v\), appears in Appendix~\ref{delta-deriv}.}
-When \(\sigma_u\) is small, deworming choices reveal more about \(v_i\), so \(\Delta(w^\ast)\) varies sharply with the cutoff. When \(\sigma_u\) is large, choices are noisy signals of type and \(\Delta(w^\ast)\) becomes flatter, pushing the multiplier toward one. Because latent utility is normalized by \(\operatorname{Var}(v_i)=1\), \(\sigma_u\) is interpreted as idiosyncratic decision noise relative to intrinsic motivation. The joint likelihood pins down the implied social image return, \(\lambda p_{\mathrm{Observed}}(z,d)\Delta(w^\ast)\), and how it changes with distance. These are the objects needed for the unitless social multiplier.
+When \(\sigma_u\) is small, deworming choices reveal more about \(v_i\), so \(\Delta(w^\ast)\) varies sharply with the cutoff. When \(\sigma_u\) is large, choices are noisy signals of type and \(\Delta(w^\ast)\) becomes flatter, pushing the multiplier toward one. Because latent utility is normalized by \(\operatorname{Var}(v_i)=1\), \(\sigma_u\) is interpreted as idiosyncratic decision noise relative to intrinsic motivation. Empirically, \(\sigma_u\) is disciplined by the curvature of take-up across arm-by-distance cells together with the nonlinear normal signal-extraction restriction above: changing \(\sigma_u\) changes both the level and curvature of \(\Delta(w^\ast)\), so it changes the social-image return needed to rationalize the observed take-up gradients at the estimated observability probabilities. The prior regularizes this scale parameter, and Appendix~\ref{priors} reports prior predictive checks. The joint likelihood pins down the implied social image return, \(\lambda p_{\mathrm{Observed}}(z,d)\Delta(w^\ast)\), and how it changes with distance. These are the objects needed for the unitless social multiplier.
 
 \emph{Remaining parameters.}
 Conditional on the relative valuation parameter \(\psi\), the noise parameter \(\sigma_u\), and the observability parameters \(\beta^O_z\) and \(\gamma^O_z\), the remaining take-up parameters are identified through the take-up likelihood. The distance cost coefficient \(\delta\) is identified by distance induced variation in take-up. The social image weight \(\lambda\) is identified on the normalized latent utility scale by how take-up varies with estimated observability across item/signal arms and distances. The arm specific non social payoff coefficients \(\beta_z\) are identified by residual take-up differences across item/signal arms, subject to the bracelet versus calendar relative valuation restriction.
@@ -892,7 +893,7 @@ The resulting posterior distribution is also the input for the policy exercise i
 
 \subsection{Structural Results}
 
-Appendix Table~\ref{tab:struct-model-params} reports posterior estimates for the full parameter vector. The key parameters have direct economic interpretations. The parameter \(\delta\) is the marginal disutility of distance. The parameter \(\lambda\) converts observability into social image utility. The parameter \(\sigma_u\) governs how noisy actions are as signals of intrinsic motivation. The parameter \(\psi\) helps pin down the relative private value of the calendar and bracelet. The estimates imply a positive weight on social image, a positive distance cost, and a negative private value for ink.
+Appendix Table~\ref{tab:struct-model-params} reports posterior estimates for the full parameter vector. The key parameters have direct economic interpretations. The parameter \(\delta\) is the marginal disutility of distance. The parameter \(\lambda\) converts observability into social image utility. The parameter \(\sigma_u\) governs how noisy actions are as signals of intrinsic motivation. The parameter \(\psi\) helps pin down the relative private value of the calendar and bracelet; the estimate implies that the calendar is privately valued more than the bracelet in the endline gift-choice exercise. The estimates also imply a positive weight on social image, a positive distance cost, and a negative private value for ink.
 
 \subsubsection{Average Treatment Effects}
 
@@ -1660,7 +1661,7 @@ This table reports estimates from a linear probability model (OLS) with a binary
     % }
 \floatfoot{\textit{Notes:} 
 The table reports posterior means and 95 percent credible intervals from the willingness to pay (WTP) submodel, one of the three blocks of the structural estimation (see Section \ref{structural}). 
-Private utility parameters are restricted to be equal, \( \beta_{\text{calendar}}=\beta_{\text{bracelet}} \), so the scalar \( \psi \) measures the mean difference (KSh) in respondents’ valuation of a calendar relative to a bracelet (Panel A). 
+Private utility parameters are restricted to be equal, \( \beta_{\text{calendar}}=\beta_{\text{bracelet}} \), so the scalar \( \psi \) measures the mean difference (USD) in respondents' valuation of a calendar relative to a bracelet (Panel A).
 Panel B gives posterior predicted probabilities that a respondent prefers the calendar when the switch offer is +50, 0, and –50KSh. The two‑stage probit likelihood is detailed in Appendix \ref{sec:wtp-model}. The structural model uses observations from the take-up analysis sample ($N$=9{,}805), the WTP sample ($N$=467), and endline observability sample ($N$=1{,}141).}
       %Survey wording: respondents first chose between the items (``Which one would you like— the calendar or the bracelet?’’), then faced a randomly drawn cash offer \(x\) (``You can take the [bracelet/calendar] on its own, \emph{or} the [calendar/bracelet] and \(x\) KSh’’).}
   \end{threeparttable}
@@ -1695,7 +1696,7 @@ Panel B gives posterior predicted probabilities that a respondent prefers the ca
   \begin{threeparttable}
     \input{tables/all-structural-parameters-table-fit-1-STRUCTURAL_LINEAR_U_SHOCKS_PHAT_MU_REP_FOB}
 \floatfoot{\textit{Notes:} 
-This table reports posterior means and 95 percent credible intervals from the joint Bayesian estimation in Section~\ref{structural}. The model is estimated in Stan using Hamiltonian Monte Carlo with four chains, 400 warm up draws, and 400 saved draws per chain. Sampler diagnostics show no divergent transitions and split \(\hat R<1.1\) for all parameters. \textit{Take-up parameters.} The parameter \(\lambda\) is the social image weight: it scales the probability that an individual's deworming status is observed into the utility payoff from being seen to deworm. The arm specific private payoff parameters \(\beta_z\), for \(z\in\{\text{Control},\text{Ink},\text{Calendar},\text{Bracelet}\}\), capture non social utility shifts associated with each assignment arm, including any private item value. The calendar and bracelet private payoff parameters are linked through the relative valuation restriction described in Section~\ref{structural}. The parameter \(\delta\) is the marginal disutility of distance to the treatment site. The parameter \(\sigma_u\) is the standard deviation of the idiosyncratic utility shock \(u_i\). \textit{Observability parameters.} The observability parameters describe the endline belief model for whether an individual's deworming status is known in the community. The parameter \(\beta^O_{\text{Control}}\) is the Control intercept in the observability logit index, while \(\beta^O_z\), for \(z\in\{\text{Ink},\text{Calendar},\text{Bracelet}\}\), are arm specific intercept shifts relative to Control. The parameter \(\gamma^O_{\text{Control}}\) is the Control distance coefficient, while \(\gamma^O_z\), for \(z\in\{\text{Ink},\text{Calendar},\text{Bracelet}\}\), are arm specific distance shifts relative to Control. A negative \(\gamma^O_{\text{Control}}\) means that observability falls with distance in Control communities. Positive values of \(\gamma^O_{\text{Bracelet}}\) or \(\gamma^O_{\text{Ink}}\) indicate that those arms offset the distance related decline in observability. \textit{Relative valuation parameters.} The relative valuation parameters link the bracelet calendar gift choice exercise to private item values. The parameter \(\psi\) is the mean calendar minus bracelet valuation difference in the monetary units used in the elicitation. The parameter \(\rho\) maps that valuation difference into the latent utility scale used in the take-up model. The joint model combines the take-up analysis sample (\(N=9{,}805\)), the relative valuation sample (\(N=467\)), and the endline observability sample (\(N=1{,}141\)).}
+This table reports posterior means and 95 percent credible intervals from the joint Bayesian estimation in Section~\ref{structural}. The model is estimated in Stan using Hamiltonian Monte Carlo with four chains, 400 warm up draws, and 400 saved draws per chain. Sampler diagnostics show no divergent transitions; among the reported parameters, the maximum split \(\hat R\) is 1.011, the minimum bulk effective sample size is 465, and the minimum tail effective sample size is 602. \textit{Take-up parameters.} The parameter \(\lambda\) is the social image weight: it scales the probability that an individual's deworming status is observed into the utility payoff from being seen to deworm. The arm specific private payoff parameters \(\beta_z\), for \(z\in\{\text{Control},\text{Ink},\text{Calendar},\text{Bracelet}\}\), capture non social utility shifts associated with each assignment arm, including any private item value. The calendar and bracelet private payoff parameters are linked through the relative valuation restriction described in Section~\ref{structural}. The parameter \(\delta\) is the marginal disutility of distance to the treatment site. The parameter \(\sigma_u\) is the standard deviation of the idiosyncratic utility shock \(u_i\). \textit{Observability parameters.} The observability parameters describe the endline belief model for whether an individual's deworming status is known in the community. The parameter \(\beta^O_{\text{Control}}\) is the Control intercept in the observability logit index, while \(\beta^O_z\), for \(z\in\{\text{Ink},\text{Calendar},\text{Bracelet}\}\), are arm specific intercept shifts relative to Control. The parameter \(\gamma^O_{\text{Control}}\) is the Control distance coefficient, while \(\gamma^O_z\), for \(z\in\{\text{Ink},\text{Calendar},\text{Bracelet}\}\), are arm specific distance shifts relative to Control. A negative \(\gamma^O_{\text{Control}}\) means that observability falls with distance in Control communities. Positive values of \(\gamma^O_{\text{Bracelet}}\) or \(\gamma^O_{\text{Ink}}\) indicate that those arms offset the distance related decline in observability. \textit{Relative valuation parameters.} The relative valuation parameters link the bracelet calendar gift choice exercise to private item values. The parameter \(\psi\) is the mean calendar minus bracelet valuation difference in USD, matching the scaled monetary units used in the elicitation likelihood. The parameter \(\rho\) maps that valuation difference into the latent utility scale used in the take-up model. The joint model combines the take-up analysis sample (\(N=9{,}805\)), the relative valuation sample (\(N=467\)), and the endline observability sample (\(N=1{,}141\)).}
   \end{threeparttable}
 \end{table}
 
@@ -1783,7 +1784,18 @@ Table~\ref{tab:baseline-bal} reports balance tests for the 1,995 respondents in
 Panel~A of Table~\ref{tab:baseline-bal} shows that the sample is predominantly Christian (97.4\%), with a majority identifying with the Luhya ethnic group (62.3\%). Nearly half of respondents completed primary schooling (47.6\%), and 19.5\% have a floor made of tile or cement, indicating that a substantial share of the sample lives in modest material conditions. Demographic characteristics are well balanced across treatment arms: no pairwise comparison reaches statistical significance in either the close or far stratum, and all joint $F$-tests are far from conventional thresholds.
 
 \subsubsection{Deworming Knowledge}
-Panel~B of Table~\ref{tab:baseline-bal} shows that baseline deworming knowledge is high. Nearly all respondents know that children can get worms (93.4\%), and a large majority know the same is true for adults (69.9\%). Knowledge that medication treats worms is widespread at 78.2\%, and very few (1.9\%) hold the misconception that deworming is only for the sick. Prior deworming experience is also common: 68.4\% report having ever been dewormed, and 37.4\% were dewormed in the past year. Awareness of the recommended bi-yearly treatment schedule stands at 47.0\%, and 37.1\% are aware that worm infections impose externalities on others. Overall, the sample displays substantial deworming knowledge and prior engagement with deworming.
+Panel~B of Table~\ref{tab:baseline-bal} shows that baseline deworming knowledge is high. Nearly all respondents know that children can get worms (93.4\%), and a large majority know the same is true for adults (69.9\%). Knowledge that medication treats worms is widespread at 78.2\%, and very few (1.9\%) hold the misconception that deworming is only for the sick. Prior deworming experience is also common: 68.4\% report having ever been dewormed, and 37.4\% were dewormed in the past year. The ``ever dewormed'' indicator equals one for respondents who report any prior deworming, while ``dewormed in the past year'' equals one for respondents reporting treatment in the previous 1--12 months. Awareness of the recommended bi-yearly treatment schedule stands at 47.0\%, and 37.1\% are aware that worm infections impose externalities on others. Overall, the sample displays substantial deworming knowledge and prior engagement with deworming.
+
+\begin{table}[ht!]
+\scriptsize
+\caption{Baseline Prior Deworming and the Public-Signal Distance Interaction}
+\label{tab:prior-deworming-robustness}
+\centering
+\begin{threeparttable}
+\input{tables/prior-deworming-robustness}
+\floatfoot{\textit{Notes:} The table reports linear probability models of take-up on the public-signal by Far interaction. Public signal equals one for Bracelet or Ink assignment and zero for Control or Calendar assignment. Columns (1)--(2) use monitored administrative deworming as the outcome. Columns (3)--(4) use endline self-reported deworming as the outcome. Baseline prior-deworming shares are community-level shares computed from baseline survey respondents, because the baseline and take-up samples cannot be linked at the individual level. Main covariates are age, gender, and expected community distance. Standard errors are clustered by community.}
+\end{threeparttable}
+\end{table}
 
 
 \subsubsection{Social Image Concerns}
@@ -1887,7 +1899,7 @@ We select clusters using a sequential acceptance--rejection algorithm. Let \(C\)
 For a candidate cluster \(i\) and an accepted set \(R\), define the non-overlapping catchment of \(i\) as the portion of \(i\)'s 2.5~km catchment circle that does not lie within the larger buffer areas used for overlap checks around already selected PoTs. We operationalize the size of this non-overlapping catchment by counting available primary school neighborhoods. Specifically, a school neighborhood counts as available if at least 60\% of the 1~km radius circle around the school lies within the non-overlapping portion of the 2.5~km catchment. We require each selected cluster to retain at least one such available school neighborhood, i.e., \(\text{ThresholdClusterSize}=1\). Intuitively, this ensures that overlap restrictions do not leave a cluster with an effectively empty area from which to list eligible communities.
 
 \paragraph{Buffers used for overlap checks.}
-Although the 2.5~km radius defines the eligibility region for selecting study communities, overlap restrictions are evaluated using larger buffers to create separation between nearby PoTs. For the purpose of the site selection algorithm, candidate PoTs are assigned a buffer radius of either 3~km or 4~km. This auxiliary buffer assignment is not a treatment. It is used only for site selection and is independent of the Close/Far assignment. Figure~\ref{fig:cluster-overlap-schematic} illustrates the geometry: the shaded region represents the portion of a selected PoT's 2.5~km catchment that is excluded because it falls within a neighboring PoT's buffer. Study communities are selected from the remaining, non-overlapping portion of the 2.5~km catchment. As a result, any selected study community is at least 0.5~km closer to its assigned PoT than to the nearest alternative PoT.
+Although the 2.5~km radius defines the eligibility region for selecting study communities, overlap restrictions are evaluated using larger buffers to create separation between nearby candidate PoT centers. For the purpose of the site selection algorithm, candidate PoTs are assigned a buffer radius of either 3~km or 4~km. This auxiliary buffer assignment is not a treatment. It is used only for site selection and is independent of the Close/Far assignment. Figure~\ref{fig:cluster-overlap-schematic} illustrates the geometry: the shaded region represents the portion of a selected candidate PoT center's 2.5~km catchment that is excluded because it falls within a neighboring candidate center's buffer. Study communities are selected from the remaining, non-overlapping portion of the 2.5~km catchment. As a result, any selected study community is at least 0.5~km closer to its assigned candidate PoT center than to the nearest alternative candidate center. Final implementation uses mapped PoT locations near the candidate centers. In the realized implementation data, the assigned finalized PoT is the closest deworming site for all 144 selected communities; two far clusters have a nearest-alternative margin slightly below 0.5~km, reflecting small differences between candidate centers and finalized PoT coordinates.
 
 \subsubsection*{Close/Far assignment}
 
@@ -1897,7 +1909,7 @@ After PoT selection, clusters are randomly assigned, stratified by county, to Cl
 
 Within each selected cluster, we choose an anchor primary school from the geo-coded school frame to guide field listing. The anchor school is chosen so that nearby listed communities are likely to fall in the cluster's assigned distance band. In Close clusters, the anchor is typically the candidate PoT center. In Far clusters, the anchor is a primary school in the outer ring, chosen so that listed communities are likely to lie 1.25--2.5~km from the candidate PoT center.
 
-Enumerators list eligible communities near the anchor school, and we randomly select one as the study's targeted community. During field mapping, the final PoT location is identified near the candidate PoT center. Eligibility is then verified using the distance between the selected community's centroid and the finalized PoT. If the finalized PoT location or initially selected community made the assigned distance band infeasible, the PoT-community pairing was re-randomized before implementation using the same protocol and eligibility criteria. In all but two clusters, the assigned finalized PoT is the closest deworming site to the selected community.
+Enumerators list eligible communities near the anchor school, and we randomly select one as the study's targeted community. During field mapping, the final PoT location is identified near the candidate PoT center. Eligibility is then verified using the distance between the selected community's centroid and the finalized PoT. If the finalized PoT location or initially selected community made the assigned distance band infeasible, the PoT-community pairing was re-randomized before implementation using the same protocol and eligibility criteria. In the realized assignment, the assigned finalized PoT is the closest deworming site to every selected community.
 
 The experiment involved six re-randomizations prior to implementation, driven by field constraints such as the feasibility of treatment locations. All re-randomizations were conducted before any outcome data were observed, followed the same pre-specified protocol, and were restricted to eligible PoT-community pairings under the original design.
 
@@ -1911,7 +1923,7 @@ The initial randomization of the 158 selected candidate clusters assigned 80 to
 
 \subsubsection*{Potential distance balance under constrained PoT selection}
 
-A potential concern is that the acceptance-rejection algorithm may have introduced systematic differences in the set of feasible distances for communities assigned to Close versus Far, complicating the interpretation of realized continuous distance. Following \citet{borusyak2023nonrandom}, we assess this by simulating the realized selection and assignment mechanism. Holding the set of surveyed communities fixed, we re-run the full PoT selection and assignment algorithm 500 times and compute the centroid-to-PoT distances that would be induced under these counterfactual PoT-selection and assignment re-draws. We then compare the resulting potential distance distributions for communities assigned to Close and Far in the realized experiment. Figure~\ref{fig:bh-dens-plot} shows that these distributions are statistically indistinguishable, providing no evidence that the constrained algorithm generated systematic differences in potential distance profiles across realized assignment groups.
+A potential concern is that the acceptance-rejection algorithm may have introduced systematic differences in the set of feasible distances for communities assigned to Close versus Far, complicating the interpretation of realized continuous distance. Following \citet{borusyak2023nonrandom}, we assess this by simulating the realized selection and assignment mechanism. Holding the set of surveyed communities fixed, we re-run the full PoT selection, Close/Far assignment, targeted-community pairing, and feasibility-screening algorithm 500 times. In each simulated draw, each realized surveyed community receives the centroid-to-PoT distance it would have had under the feasible simulated PoT-community pairing in which it is selected. We then pool these community-level potential distances, weighting each realized surveyed community equally within each simulation draw, and compare the resulting potential distance distributions for communities assigned to Close and Far in the realized experiment. Figure~\ref{fig:bh-dens-plot} shows that these distributions are statistically indistinguishable, providing no evidence that the constrained algorithm generated systematic differences in potential distance profiles across realized assignment groups.
 
 
 \begin{figure}[htbp]
@@ -1919,7 +1931,7 @@ A potential concern is that the acceptance-rejection algorithm may have introduc
   \includegraphics[width=4.8in]{misc-figures/counterfactual-assignment-density-close-far-dens.pdf}
   \caption{Potential Distance Distributions under Constrained Randomization}
   \label{fig:bh-dens-plot}
-\floatfoot{\textit{Notes:} The figure plots potential community-centroid-to-PoT distance distributions implied by 500 re-runs of the full PoT selection and assignment algorithm \citep{borusyak2023nonrandom}, holding fixed the set of surveyed communities. Red (blue) curves correspond to communities assigned to Close (Far) in the realized experiment. The plotted distances are counterfactual potential distances under simulated re-draws, not the realized Close/Far treatment distances. A Kolmogorov--Smirnov test fails to reject equality of the two distributions (\(p=0.501\)), indicating no evidence that communities assigned to Close and Far differ in their potential distance profiles under the constrained randomization.}
+\floatfoot{\textit{Notes:} The figure plots potential community-centroid-to-PoT distance distributions implied by 500 re-runs of the full PoT selection and assignment algorithm \citep{borusyak2023nonrandom}, holding fixed the set of surveyed communities. Red (blue) curves correspond to communities assigned to Close (Far) in the realized experiment. The plotted unit is the realized surveyed community by simulation draw, weighted equally, and the distances are counterfactual potential distances under simulated feasible PoT-community pairings rather than the realized Close/Far treatment distances. A Kolmogorov--Smirnov test comparing these community-draw potential-distance distributions fails to reject equality (\(p=0.501\)), indicating no evidence that communities assigned to Close and Far differ in their potential distance profiles under the constrained randomization.}
 \end{figure}
 
 
@@ -1935,14 +1947,14 @@ A potential concern is that the acceptance-rejection algorithm may have introduc
 \subsubsection{Continuous Distance Randomization Inference}
 \label{sec:distance-random}
 
-As an additional check that our continuous distance measure is orthogonal to pre-treatment characteristics, we regress each baseline covariate on the distance from the community centroid to its assigned point of treatment (PoT), controlling for county fixed effects and clustering standard errors at the community level. We then permute community-level distances across communities within county strata 500 times and recompute the associated test statistic. Figure~\ref{fig:ri-baseline} plots the resulting randomization distribution for each covariate under the null of no association. The vertical line marks the realized value, and permutation \(p\)-values based on the 500 draws are reported in the upper right-hand corner of each panel. For all 15 covariates, the $p$-value exceeds 0.10, indicating that realized centroid-to-PoT distance is not systematically related to observable characteristics.
+As an additional check that our continuous distance measure is orthogonal to pre-treatment characteristics, we regress each baseline covariate on the distance from the community centroid to its assigned point of treatment (PoT), controlling for county fixed effects and clustering standard errors at the community level. We then use the constrained assignment simulations described above to construct placebo continuous-distance draws. For each placebo draw, each surveyed community receives a centroid-to-PoT distance sampled from the feasible PoT-community pairings generated for that community by re-running the PoT selection, Close/Far assignment, targeted-community pairing, and feasibility-screening algorithm. We then recompute the associated test statistic. Figure~\ref{fig:ri-baseline} plots the resulting placebo distribution for each covariate under the null of no association. The vertical line marks the realized value, and simulation-based \(p\)-values based on the 500 draws are reported in the upper right-hand corner of each panel. For all 15 covariates, the $p$-value exceeds 0.10, indicating that realized centroid-to-PoT distance is not systematically related to observable characteristics.
 
 \begin{figure}[htbp]
   \centering
   \includegraphics[width=5.2in]{figures/dist-ri-plot.pdf}
   \caption{Randomization Inference: Baseline Covariates and Centroid-to-PoT Distance}
   \label{fig:ri-baseline}
-   \floatfoot{\textit{Notes:} Each histogram shows the randomization distribution of the OLS \(t\)-statistic from 500 within-county permutations of community centroid-to-PoT distances. Regressions include county fixed effects. Standard errors are clustered by community. The vertical line is the realized statistic from the actual experiment, and each panel reports the corresponding randomization-inference \(p\)-value.}
+   \floatfoot{\textit{Notes:} Each histogram shows the placebo distribution of the OLS \(t\)-statistic from 500 counterfactual continuous-distance draws based on simulated feasible PoT-community pairings under the constrained assignment algorithm. Regressions include county fixed effects. Standard errors are clustered by community. The vertical line is the realized statistic from the actual experiment, and each panel reports the corresponding simulation-based \(p\)-value.}
 \end{figure}
 
 \FloatBarrier
@@ -2276,11 +2288,11 @@ Let
 \[
 g_i^{\text{val}} \equiv U_{c,i}-U_{b,i}
 \]
-denote respondent \(i\)'s latent private value of the calendar relative to the bracelet, measured in the monetary units used in the elicitation. We assume
+denote respondent \(i\)'s latent private value of the calendar relative to the bracelet, measured in US dollars, the scaled monetary units used in the elicitation likelihood. We assume
 \[
 g_i^{\text{val}}\sim N(\psi,1),
 \]
-where \(\psi\) is the mean calendar minus bracelet value and the variance is normalized for scale.
+where \(\psi\) is the mean calendar minus bracelet value in US dollars and the variance is normalized for scale.
 
 \paragraph{Stage 1: item choice.}
 Respondent \(i\) first chooses between a calendar and a bracelet. Define
@@ -2359,6 +2371,14 @@ The priors on the arm specific take-up coefficients are centered at zero, reflec
 
 The parameters \(\rho\) and \(\sigma_u\) require additional regularization. The parameter \(\rho\) maps monetary values from the relative valuation exercise into the latent utility units of the take-up model. Because this scale conversion is weakly disciplined by the data, we use a positive regularizing prior. The parameter \(\sigma_u\) governs how noisy deworming choices are as signals of intrinsic motivation. We use an inverse Gamma prior to keep this scale parameter away from degenerate values near zero. Prior predictive checks, described in Section~\ref{structural}, assess whether these priors mechanically impose the posterior patterns.
 
+\begin{figure}[ht!]
+\centering
+\includegraphics[width=0.62\linewidth]{figures/sigma-u-prior-posterior.pdf}
+\caption{Prior and Posterior Distributions for \(\sigma_u\)}
+\label{fig:sigma-u-prior-posterior}
+\floatfoot{\textit{Notes:} The prior distribution is computed from prior-predictive draws and the posterior distribution from the baseline structural model. Dashed vertical lines mark medians. The posterior median is 0.278, compared with a prior median of 0.374.}
+\end{figure}
+
 \newpage
 
 \subsection{Alternative Structural Models}
@@ -2425,7 +2445,7 @@ The centroid may mismeasure cost in communities that are geographically spread o
     % \resizebox{\textwidth}{!}{%
     \input{tables/fit105/indiv-dist-community-fp-indiv-vis-robust-struct-overall-te-table}
 	% }
-    \floatfoot{\textit{Notes:} Estimates are from the same structural specification as Table~\ref{tab:struct_overall_effects}, except that the private cost term uses each household's own distance to the point of treatment while social image returns are computed at the community centroid. Point estimates are posterior means and parentheses contain 95 percent credible intervals. The Control row reports the model implied Control mean in columns (1)--(3) and the Control Far--Close difference in column (4). The final two rows report posterior differences in expected take-up: Any Signal minus No Signal and Bracelet minus Calendar. The structural model combines observations from the take-up analysis sample (\(N=9{,}805\)), the relative valuation sample (\(N=467\)), and the endline observability sample (\(N=1{,}141\)).}
+    \floatfoot{\textit{Notes:} Estimates are from the same structural specification as Table~\ref{tab:struct_overall_effects}, except that the private cost term uses each household's own distance to the point of treatment while social image returns are computed at the community centroid. Point estimates are posterior means and parentheses contain 95 percent credible intervals. The Control row reports the model implied Control mean in columns (1)--(3) and the Control Far--Close difference in column (4). The final two rows report posterior differences in expected take-up: Any Signal minus No Signal and Bracelet minus Calendar. The structural model combines observations from the take-up analysis sample (\(N=9{,}805\)), the relative valuation sample (\(N=467\)), and the endline observability sample (\(N=1{,}141\)). Sampler diagnostics for this robustness fit show no divergent transitions; among the reported parameters, the maximum split \(\hat R\) is 1.022, the minimum bulk effective sample size is 221, and the minimum tail effective sample size is 212.}
 \end{threeparttable}
 \end{table}
 
@@ -2438,7 +2458,7 @@ The centroid may mismeasure cost in communities that are geographically spread o
     % \resizebox{\textwidth}{!}{%
     \input{tables/fit105/indiv-dist-indiv-fp-robust-struct-overall-te-table}
 	% }
-  \floatfoot{\textit{Notes:} Estimates are from the same structural specification as Table~\ref{tab:struct_overall_effects}, except that both the private cost term and the fixed point calculation for social image returns are based on each household's own distance to the treatment site. Point estimates are posterior means and parentheses contain 95 percent credible intervals. The Control row reports the model implied Control mean in columns (1)--(3) and the Control Far--Close difference in column (4). The final two rows report posterior differences in expected take-up: Any Signal minus No Signal and Bracelet minus Calendar. The structural model combines observations from the take-up analysis sample (\(N=9{,}805\)), the relative valuation sample (\(N=467\)), and the endline observability sample (\(N=1{,}141\)).}
+  \floatfoot{\textit{Notes:} Estimates are from the same structural specification as Table~\ref{tab:struct_overall_effects}, except that both the private cost term and the fixed point calculation for social image returns are based on each household's own distance to the treatment site. Point estimates are posterior means and parentheses contain 95 percent credible intervals. The Control row reports the model implied Control mean in columns (1)--(3) and the Control Far--Close difference in column (4). The final two rows report posterior differences in expected take-up: Any Signal minus No Signal and Bracelet minus Calendar. The structural model combines observations from the take-up analysis sample (\(N=9{,}805\)), the relative valuation sample (\(N=467\)), and the endline observability sample (\(N=1{,}141\)). Sampler diagnostics for this robustness fit show two divergent transitions; among the reported parameters, the maximum split \(\hat R\) is 1.022, the minimum bulk effective sample size is 142, and the minimum tail effective sample size is 132.}
 \end{threeparttable}
 \end{table}
 
@@ -2453,7 +2473,7 @@ The centroid may mismeasure cost in communities that are geographically spread o
     % \resizebox{\textwidth}{!}{%
     \input{tables/fit105/struct-robustness-nooutliers-overall-te-table.tex}
 	% }
-    \floatfoot{\textit{Notes:} Estimates are based on the same structural specification as Table~\ref{tab:struct_overall_effects}, except that clusters whose mean squared household to centroid distance exceeds \(0.5\text{ km}^{2}\) are excluded from the sample. Point estimates are posterior means and parentheses contain 95 percent credible intervals. The Control row reports the model implied Control mean in columns (1)--(3) and the Control Far--Close difference in column (4). The final two rows report posterior differences in expected take-up: Any Signal minus No Signal and Bracelet minus Calendar. The structural model combines observations from the retained take-up, relative valuation, and endline observability samples.}
+    \floatfoot{\textit{Notes:} Estimates are based on the same structural specification as Table~\ref{tab:struct_overall_effects}, except that clusters whose mean squared household to centroid distance exceeds \(0.5\text{ km}^{2}\) are excluded from the sample. Point estimates are posterior means and parentheses contain 95 percent credible intervals. The Control row reports the model implied Control mean in columns (1)--(3) and the Control Far--Close difference in column (4). The final two rows report posterior differences in expected take-up: Any Signal minus No Signal and Bracelet minus Calendar. The structural model combines observations from the retained take-up, relative valuation, and endline observability samples. Sampler diagnostics for this robustness fit show no divergent transitions; among the reported parameters, the maximum split \(\hat R\) is 1.018, the minimum bulk effective sample size is 355, and the minimum tail effective sample size is 309.}
 \end{threeparttable}
 \end{table}
 
@@ -2500,7 +2520,7 @@ The main specification assumes that the composite type \(w=v+u\) is Gaussian. Th
 With observability held fixed at a positive level, a social multiplier different from one requires a type inference function that is not locally flat, \(\Delta'(w)\neq0\). More generally, when observability varies with distance, changes in either \(\mu(z,d)\) or \(\Delta(w^\ast)\) can generate a multiplier different from one. The purpose of the simulations below is to show that the multiplier is not specific to the Gaussian benchmark. It equals one only over regions where the induced type inference function is locally flat.
 
 \paragraph{Simulation design.}
-We compare two type distributions. The first is the Gaussian benchmark, \(w\sim N(0,1)\). The second is an equally weighted bimodal mixture of \(N(-\eta,1)\) and \(N(\eta,1)\), with \(\eta\) large enough that the two modes are distinct. For each distribution, we vary the net private benefit shifter \(b\) and compute (i) the equilibrium participation rate \(\bar y(b)\), (ii) the social image return \(\Delta(w^\ast(b))\), and (iii) the implied social multiplier \(M(b)\).
+We compare two type index distributions on the scale shown in Figure~\ref{fig:robustness-bimodal}. The first is a concentrated Gaussian benchmark, approximately \(w\sim N(0.4,0.2^2)\). The second is an asymmetric bimodal mixture, approximately \(0.25\,N(-2,0.2^2)+0.75\,N(0.4,0.2^2)\). For each distribution, we vary the net private benefit shifter \(b\) and compute (i) the equilibrium participation rate \(\bar y(b)\), (ii) the social image return \(\Delta(w^\ast(b))\), and (iii) the implied social multiplier \(M(b)\).
 
 \paragraph{Results.}
 Figure~\ref{fig:robustness-bimodal} shows the results. Under the bimodal distribution, there are intervals in which the cutoff moves through the low density gap between the two modes. In those intervals, the participation curve is locally flat, \(\Delta(w^\ast)\) is locally constant, and the social multiplier equals one. Outside these locally flat regions, the multiplier differs from one. Under the Gaussian benchmark, there is no such gap, and \(\Delta(w^\ast)\) varies smoothly with the cutoff.
@@ -2536,7 +2556,7 @@ These simulations clarify the role of the distributional assumption. The multipl
   \caption{Simulation under Gaussian and Bimodal Type Distributions}
   \label{fig:robustness-bimodal}
   \floatfoot{\textit{Notes:}  
-    Panel (a) plots the assumed densities for \(w\): Gaussian in red and bimodal in blue. Panel (b) shows the participation rate as the net private benefit shifter \(b\) changes. Panel (c) traces the implied social image return. Panel (d) gives the resulting social multiplier. Flat segments, visible only in the bimodal case, occur where the cutoff type moves through the low density gap between the two modes. In those intervals, \(\Delta(w^\ast)\) is locally constant and \(M(b)\) equals one.}
+    Panel (a) plots the assumed densities for the type index \(w\): a concentrated Gaussian benchmark in red, approximately \(N(0.4,0.2^2)\), and an asymmetric bimodal mixture in blue, approximately \(0.25\,N(-2,0.2^2)+0.75\,N(0.4,0.2^2)\). The axis label \(V^\ast\) in the plotted figure denotes the cutoff/type index scale \(w^\ast\) used in the text. Panel (b) shows the participation rate as the net private benefit shifter \(b\) changes. Panel (c) traces the implied social image return. Panel (d) gives the resulting social multiplier. Flat segments, visible only in the bimodal case, occur where the cutoff type moves through the low density gap between the two modes. In those intervals, \(\Delta(w^\ast)\) is locally constant and \(M(b)\) equals one.}
 \end{figure}
 
 
