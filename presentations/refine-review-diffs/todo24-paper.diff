@@ -869,1 +869,1 @@
-The parameter \(\sigma_u\) governs how informative deworming decisions are about intrinsic motivation. We normalize \(v_i\sim N(0,1)\) and assume \(u_i\sim N(0,\sigma_u^2)\), so \(w_i=v_i+u_i\sim N(0,1+\sigma_u^2)\). For the normal mixture, the type inference term is
+The parameter \(\sigma_u\) governs how informative deworming decisions are about intrinsic motivation. We normalize \(v_i\sim N(0,1)\) and assume \(u_i\sim N(0,\sigma_u^2)\), so \(w_i=v_i+u_i\sim N(0,1+\sigma_u^2)\). Under this normal-sum specification, the type inference term is
