# Local scratch space

This directory is for disposable local experiments only. Supported code must
live in `R/`, `scripts/`, `hpc/`, or `tests/`, and production code must never
reference a path below `scratch/`.
